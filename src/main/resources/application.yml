spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/student4?characterEncoding=utf-8&serverTimezone=Asia/Shanghai
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
mybatis:
  config-location: classpath:mybatis/mybatis-config.xml
logging:
  level:
    com:
     galaxy:
       mapper: debug
